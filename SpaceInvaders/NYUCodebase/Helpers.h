#pragma once
#include "theSDLheaders.h"

unsigned getKey();
/*
void renderPause(SDL_Window*);

void renderStart(SDL_Window*);

void renderWin(SDL_Window*);

void renderLose(SDL_Window*);
*/